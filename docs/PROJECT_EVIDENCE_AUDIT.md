# 项目证据审计

更新时间：2026-07-16

本文档记录 `content/projects.json` 中项目的真实交付成熟度。项目文案、训练路径引用和简历表达不能替代可运行代码与验收证据。

## 成熟度协议

| 状态 | 含义 | 必要证据 |
| --- | --- | --- |
| `blueprint` | 只有目标、架构与交付模板 | 不允许展示 Demo、测试通过或已交付表述 |
| `prototype` | 存在可运行的最小闭环 | Demo 路径、验证日期、至少 1 条命令和 2 项验证产物 |
| `verified` | 可独立部署并完成验收 | `prototype` 全部证据，加独立仓库地址与完整验收记录 |

`resumeBullet` 在 `verified` 前只能显示为目标表达，不得直接用于简历。

## 当前审计结果

| 项目 | 状态 | 已有证据 | 主要缺口 |
| --- | --- | --- | --- |
| Prompt 调试与回归台 | `prototype` | 固定响应夹具、Schema/业务规则评估、单测、双端交互页 | 接入真实模型、独立运行配置、真实延迟与成本、部署验收 |
| RAG 企业知识库 | `blueprint` | 需求、架构、测试和部署模板 | 摄取流水线、检索评估集、API、前端、测试与部署均未实现 |
| 多步骤任务规划 Agent | `blueprint` | 需求、架构、测试和部署模板 | 状态机、工具协议、恢复机制、轨迹评估和部署均未实现 |
| Agent 评测控制台 | `blueprint` | 需求、架构、测试和部署模板 | 数据集、评测执行器、指标、对比界面和部署均未实现 |
| MCP 工具服务器 | `blueprint` | 需求、架构、测试和部署模板 | MCP 服务、工具 Schema、权限边界、契约测试和部署均未实现 |
| AI 客服工作流 | `blueprint` | 需求、架构、测试和部署模板 | 路由、RAG、人工升级、可观测性、回放和部署均未实现 |

## Prompt 原型边界

入口：`/labs/prompt-regression`

- 只使用仓库内固定输入、期望和响应，不调用模型，也不需要 API Key。
- 同时计算 Schema 通过率与业务通过率，避免把“JSON 可解析”误当作“回答正确”。
- 延迟和成本是固定夹具字段，只验证报表与回归流程，不代表真实模型性能。
- 通过 `npm run test:unit` 和 `npm run build` 验证。

## 下一验收门禁

下一条唯一主线是把 `rag-knowledge-base` 从 `blueprint` 提升为 `prototype`：

1. 使用仓库内小型确定性文档集，先实现摄取、切分、检索和引用返回闭环。
2. 建立至少 10 条检索评估样例，明确 Hit@K、MRR 或等价指标。
3. 提供可运行页面或 API、单元测试、失败样例和启动命令。
4. 完成桌面与 390px 手机端验收后，才能把状态改为 `prototype`。
5. 未达到门禁前，不增加新项目数量、不接登录和数据库。
