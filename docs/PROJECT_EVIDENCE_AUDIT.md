# 项目证据审计

更新时间：2026-07-16

本文档记录 `content/projects.json` 中项目的真实交付成熟度。项目文案、训练路径引用和简历表达不能替代可运行代码与验收证据。

## 成熟度协议

| 状态 | 含义 | 必要证据 |
| --- | --- | --- |
| `blueprint` | 只有目标、架构与交付模板 | 不允许展示 Demo、测试通过或已交付表述 |
| `prototype` | 存在可运行的最小闭环 | Demo 路径、验证日期、至少 1 条命令和 2 项验证产物 |
| `verified` | 可独立部署并完成验收 | `prototype` 全部证据，加独立仓库地址与完整验收记录 |

`resumeBullet` 在 `verified` 前只能显示为目标表达，不得直接用于简历。

## 当前审计结果

| 项目 | 状态 | 已有证据 | 主要缺口 |
| --- | --- | --- | --- |
| Prompt 调试与回归台 | `prototype` | 固定响应夹具、Schema/业务规则评估、单测、双端交互页 | 接入真实模型、独立运行配置、真实延迟与成本、部署验收 |
| RAG 企业知识库 | `prototype` | 8 份版本化手册、16 个章节块、混合检索、稳定引用、13 条评估题、单测和双端页面 | 向量数据库、真实 Embedding、生成模型、权限过滤和独立部署尚未实现 |
| 多步骤任务规划 Agent | `prototype` | 服务端运行 API、两个真实只读内容工具、严格 Schema、权限与四步预算、Responses 适配器、20 条规划契约、五类安全轨迹 | 真实模型生产实测、持久化恢复、写工具补偿、匿名限流和独立部署尚未完成 |
| Agent 评测控制台 | `prototype` | 五类固定轨迹、双策略报告、五维评分、样本级回归位置、关键安全门禁、单测和双端页面 | 真实模型 Trace、人工评分校准、延迟成本数据和独立部署尚未实现 |
| MCP 工具服务器 | `blueprint` | 需求、架构、测试和部署模板 | MCP 服务、工具 Schema、权限边界、契约测试和部署均未实现 |
| AI 客服工作流 | `blueprint` | 需求、架构、测试和部署模板 | 路由、RAG、人工升级、可观测性、回放和部署均未实现 |

## Prompt 原型边界

入口：`/labs/prompt-regression`

- 只使用仓库内固定输入、期望和响应，不调用模型，也不需要 API Key。
- 同时计算 Schema 通过率与业务通过率，避免把“JSON 可解析”误当作“回答正确”。
- 延迟和成本是固定夹具字段，只验证报表与回归流程，不代表真实模型性能。
- 通过 `npm run test:unit` 和 `npm run build` 验证。

## RAG 原型边界

入口：`/labs/rag-retrieval`

- 使用仓库内 8 份确定性内部手册，按 16 个稳定章节边界摄取，不连接外部数据源。
- 关键词基线使用字符词面重叠；混合检索加入稀有词权重和显式概念标签，不伪装成真实向量相似度。
- 13 条固定评估题包含 12 条有答案问题和 1 条知识库外问题，输出 Hit@3、MRR、引用覆盖和未知问题拒答率。
- 回答直接抽取命中文本并附稳定文档、版本和章节引用，不调用生成模型。
- 通过 `npm run test:unit`、`npm run build` 和双端 Playwright 验证。

## 受控 Agent 原型边界

入口：`/labs/controlled-agent`

- 页面现在包含服务端运行时与确定性安全回归两层；`POST /api/agent/run` 已实际读取站内知识与项目证据。
- 生产默认仍使用确定性服务端规划器；Responses API 适配器只有配置显式开关与服务端密钥后才启用。
- 两个真实工具均为只读，使用严格参数 Schema、最小权限和最多四步预算；当前没有写操作或补偿事务。
- 每次工具调用先检查权限和步数预算；高风险删除必须等待人工审批。
- 临时故障重试复用同一幂等键，只有成功结果能提交一次副作用。
- 越权场景在工具调用前失败；预算场景达到上限后不再继续循环。
- 事件轨迹包含连续序号、稳定时间、工具、尝试次数、幂等键和副作用状态。

## Agent 评测原型边界

入口：`/labs/agent-evaluation`

- V2 直接复用受控 Agent 的五类轨迹；V1 使用仓库内固定回归事实，不伪装成真实线上版本。
- 评测固定覆盖结果、轨迹、权限、预算和副作用，输出具体样本与首个回归位置。
- 门禁同时检查综合分、单样本最低分与关键安全违规；权限、预算或副作用失败不能被均分抵消。
- 当前不接模型评判、真实 Trace、延迟或成本采集，报告只证明确定性评测流程可以复算。

## 下一验收门禁

真实 JD 校准与模拟面试闭环已完成：6 个岗位样本均保留来源与访问日期，8 个能力域可映射到现有项目证据，每个岗位固定生成 5 道题并用四项确定性量表复算。该结果不提升任何项目成熟度。

“多步骤任务规划 Agent”的 `verified` 候选阶段 1 已完成：服务端运行契约、两个真实只读工具、Responses 适配器和 20 条夹具评测已经落地，但不满足 `verified`。

下一条唯一主线是继续完成阶段 2：

1. 为匿名公开 API 增加身份或持久化限流，再用生产密钥执行真实模型基线；密钥不得进入浏览器。
2. 把 20 条契约评测升级为真实模型运行样本，记录工具选择、结果正确率、延迟、Token、成本和 request id。
3. 接入持久化运行仓储，保存状态、工具结果和审批决策，支持中断后恢复、重放和版本迁移。
4. 增加一个受审批保护的写工具，验证幂等、副作用确认和失败补偿，不能用确定性夹具替代线上证据。
5. 完成独立部署、监控、回滚和线上冒烟；全部记录齐备后才允许改为 `verified`。

本阶段仍不增加项目数量。若缺少模型密钥或部署环境，可以完成接口与夹具，但不得宣称真实模型或线上验收已完成。
